import { ArticleShowPipe } from './article-show.pipe';

describe('ArticleShowPipe', () => {
  it('create an instance', () => {
    const pipe = new ArticleShowPipe();
    expect(pipe).toBeTruthy();
  });
});
